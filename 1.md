你现在是顶级前端架构师。请帮我在当前目录下从零初始化一个名为 "digital-garden" 的个人博客项目。

技术栈要求：
1. 核心框架：Astro (最新版)
2. 样式：Tailwind CSS (使用 @astrojs/tailwind 集成)
3. 交互组件：React 18 (使用 @astrojs/react 集成，后续用于写特效)
4. Markdown 支持：默认即可，需支持 Frontmatter

执行步骤：
1. 运行必要的 npm 命令创建项目（如 npm create astro@latest）。
2. 安装依赖：@astrojs/tailwind, tailwindcss, @astrojs/react, react, react-dom。
3. 配置 astro.config.mjs，引入 tailwind() 和 react()。
4. 在 src/layouts/BaseLayout.astro 创建基础布局，要求：
   - 包含 <html> 标签，默认添加 class="dark" (暗黑模式优先)。
   - 引入全局样式文件 src/styles/global.css (包含 Tailwind 的 @tailwind 指令)。
   - 包含 <slot /> 用于插入页面内容。
5. 修改 src/pages/index.astro，使用 BaseLayout，写入 "Hello Digital Garden"。

请确保项目能通过 npm run dev 正常启动。只输出需要执行的命令和需要创建/修改的文件代码，不要多余解释。
