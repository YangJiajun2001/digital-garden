要求：
1. 修改 tailwind.config.mjs：设置 darkMode: 'class'。
2. 创建 React 组件 src/components/ThemeToggle.tsx：
   - 渲染一个按钮，暗色模式显示 ☀️ 图标，亮色模式显示 🌙 图标。
   - 点击时切换 <html> 标签的 dark 类名。
   - 将当前主题保存到 localStorage，页面加载时优先读取 localStorage，如果没有则尊重系统偏好 (prefers-color-scheme)。
   - 为了防止页面加载时闪烁，请在 BaseLayout.astro 的 <head> 中添加一段内联 <script>，在 DOM 渲染前就把正确的 class 加到 <html> 上。
3. 在 BaseLayout.astro 的导航栏右侧引入 ThemeToggle 组件 (client:load)。
4. 修改 global.css：
   - 给 body 添加过渡动画：transition: background-color 0.3s ease, color 0.3s ease;
   - 确保亮色模式下基础样式为：bg-white text-gray-900；暗色模式保持现在的 bg-black text-gray-50。
   - 检查并适配之前写的毛玻璃效果和发光边框：亮色模式下，毛玻璃变为 bg-white/70 border-gray-200/50，阴影使用常规阴影；暗色模式保持发光阴影。

请给出修改后的 tailwind.config.mjs、ThemeToggle.tsx、global.css 和 BaseLayout.astro 的完整代码或关键修改部分。
