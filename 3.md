基于之前的 Astro 博客项目，重构 src/pages/index.astro，实现有创意的 Bento Grid 首页。

要求：
1. 在 index.astro 顶部通过 getStaticPaths 或 Astro.glob 获取 content/blog/ 下的所有文章数据，并按 pubDate 降序排列。
2. 页面顶部是一个 Hero 区域，包含博客名 "My Digital Garden" 和副标题 "播种想法，培育认知"。
3. 下方是 Bento Grid 布局区域：
   - 使用 Tailwind CSS 的 grid 类，例如 grid-cols-1 md:grid-cols-2 lg:grid-cols-3。
   - 卡片要有玻璃拟态效果：bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-6。
   - 卡片大小要有变化：第一篇文章 (最新的) 要跨两列，其他文章占一列。可以随机让某篇文章跨两行。
   - 卡片内容包含：
     - 文章标题 (大号字体，白色加粗)
     - 文章描述 (灰色小字，最多两行截断)
     - 底部标签和状态：状态用 emoji 表示，seed 显示 🌱，growing 显示 🌿，evergreen 显示 🌳。
     - 日期显示在右上角，小号灰色字体。
4. 点击卡片跳转到 /blog/[slug] 详情页。
5. 全局背景使用深色 (如 bg-gray-950)。

请给出完整的 src/pages/index.astro 代码，以及必要的 CSS 修改。
