现在对首页 src/pages/index.astro 的 Bento Grid 进行高保真视觉升级，消除粗糙感。

要求对文章卡片进行以下改造：

1. 3D 倾斜悬浮效果：
   创建 React 组件 src/components/TiltCard.tsx。
   - 使用 onMouseMove 计算鼠标相对卡片中心的坐标，应用 CSS transform: perspective(1000px) rotateX(...) rotateY(...)。
   - onMouseLeave 时平滑恢复原位。
   - 内部需要一个发光层，跟随鼠标位置产生高光。

2. 卡片视觉升级 (在 index.astro 或 TiltCard 中调整 Tailwind)：
   - 背景：使用深色半透明加微弱的渐变色 (例如 from-gray-900/80 to-gray-800/50)，而不是纯黑。
   - 边框：默认 border-white/5，hover 时变为 border-emerald-500/50 产生发光感。
   - 内部添加一个绝对定位的径向渐变光晕，随鼠标移动，产生类似全息卡片的质感。

3. 首页背景：
   不要纯色背景，在 global.css 中添加一些网格线背景 或微弱的噪点纹理。

4. 用 TiltCard 包裹首页的每一张文章卡片。

请提供 TiltCard.tsx 的完整代码，以及修改后的 index.astro 关键代码。
