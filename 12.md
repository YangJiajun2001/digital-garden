为博客添加一个"随机漫步"功能。我的开发系统是 Windows。

1. 在构建时生成文章 slug 列表：
   - 不要用 shell 脚本生成，而是在 Astro 的构建流程中处理。
   - 创建 src/pages/api/slugs.json.ts（或使用 Astro 的 SSR/静态生成方式），在构建时输出一个包含所有文章 slug 的 JSON 数组，路径为 /api/slugs.json。
2. 创建 React 组件 src/components/RandomStrollButton.tsx：
   - 渲染一个按钮，文案为"🎲 随机漫步"。
   - 点击时，fetch 请求 /api/slugs.json 获取所有文章 slug。
   - 从中随机选择一个 slug。
   - 使用 Astro 的 View Transitions API 进行页面跳转，避免生硬刷新。
   - 按钮 hover 时要有微动效（如轻微摇晃或发光）。
3. 将此按钮放置在 BaseLayout.astro 的导航栏右侧。

请给出 src/pages/api/slugs.json.ts（或其他实现方式）、RandomStrollButton.tsx 完整代码以及 BaseLayout.astro 的修改。
