为博客文章详情页添加顶部阅读进度条功能。

要求：
1. 创建 src/components/ReadingProgressBar.tsx (React 组件)。
2. 逻辑：
   - 监听页面滚动事件（使用 requestAnimationFrame 优化性能，不要卡顿）。
   - 计算阅读进度百分比：scrollTop / (scrollHeight - clientHeight)。
3. 视觉：
   - 固定在页面最顶部 (fixed top-0 left-0)，z-index 要足够高 (z-50)。
   - 高度为 3px (h-[3px])。
   - 宽度随进度百分比变化。
   - 背景色使用渐变色 (例如 from-emerald-500 to-cyan-500)。
   - 进度条下方带一个微弱的发光阴影效果，越接近100%越亮。
4. 引入：
   - 在 src/pages/blog/[slug].astro 中引入此组件 (client:load)。

请给出 ReadingProgressBar.tsx 的完整代码和详情页的修改。
