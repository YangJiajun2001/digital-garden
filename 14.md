对内容系统进行重大升级：增加“闪念胶囊”和“动态标签筛选”。

1. 添加“闪念胶囊”内容集合：
   - 在 content/config.ts 新增 'notes' 集合，schema 包含：title (string, 可选), content (string, 必填), pubDate (date, 必填), tags (array, 可选)。
   - 创建几条示例 notes 在 content/notes/ 下（如：关于某个技术栈的短吐槽，或者一个灵感）。
   - 创建 src/pages/notes/index.astro，以时间线形式展示这些短思考（样式区别于长文，更轻量，类似推文）。

2. 首页动态标签筛选（涵盖 blog 和 notes）：
   - 重构 src/pages/index.astro：
     - 提取 blog 和 notes 中所有不重复的标签及数量。
     - 在 Bento Grid 上方渲染一行标签按钮："全部"、"🌱 种子"、"🌿 生长中"、"🌳 常青"，以及各个自定义标签。
   - 创建 React 组件 src/components/FilterGrid.tsx：
     - 接收标签列表和所有内容数据（blog+notes）作为 props。
     - 点击标签按钮过滤：高亮选中按钮，隐藏/显示卡片时使用 CSS transition (opacity, scale)，生硬的不要。
     - 闪念胶囊在卡片列表中要有独特的视觉表现（比如没有大图，直接显示内容摘要，加一个 💡 图标）。

请给出 content/config.ts 更新、notes 页面、FilterGrid.tsx 完整代码和修改后的 index.astro。
