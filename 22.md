为标签页面 创建一个 3D 球形标签云组件。

1. 收集数据：
   - 在 src/pages/tags/index.astro 中，获取所有标签及其对应的文章数量。

2. 创建 React 组件 src/components/TagCloud3D.tsx：
   - 接收 tags 数组 (包含 name 和 count) 作为 props。
   - 使用纯数学计算 (球坐标系) 将标签均匀分布在一个 3D 球体表面上。
   - 球体自动绕 Y 轴缓慢旋转。
   - 鼠标移入球体区域时，根据鼠标位置改变旋转方向和速度（交互感）。
   - 标签的 Z 轴深度通过透明度 和缩放 来模拟近大远小。
   - 标签的字体大小根据 count (文章数量) 动态调整，文章越多的标签越大。
   - 点击标签跳转到 /tags/[tag] 页面。
   - 颜色使用 Tailwind 的 emerald/cyan 渐变色系。

3. 修改标签页面：
   - 页面标题 "🏷️ 标签星图"
   - 引入 TagCloud3D 组件，占据页面主要视觉区域。

请给出 TagCloud3D.tsx 的完整代码，以及修改后的 tags/index.astro。
